class Task {
  final int id, cantidad;
  final String nombreproducto, descripcion, categoria;

  Task({
    required this.id,
    required this.nombreproducto,
    required this.descripcion,
    required this.categoria,
    required this.cantidad,
  });
}
